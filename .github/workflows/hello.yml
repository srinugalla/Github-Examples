name: Hello-Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello - ✅ GitHub Actions is working in my fork!"
