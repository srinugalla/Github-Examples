name: Fork Test Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "✅ GitHub Actions is working in my fork!"
